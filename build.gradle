plugins {
    id 'performance'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

performance {
    simulationClass = 'simulations.Aip_Simulation'
    transactionNamesToGraph = ['AIP_340_CheckYourAnswers', 'AIP_350_AppealOverview',
                                   'CCD_SubmitEvent_requestHomeOfficeData', 'CCD_SubmitEvent_requestRespondentEvidence']
}
